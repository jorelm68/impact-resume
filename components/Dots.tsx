import React from 'react';
import View from "./View";

export default function Dots() {
    return (
        <View style={{
            width: '1.5rem',
            overflow: 'hidden',
            backgroundColor: 'blue',
        }} />
    );
}
