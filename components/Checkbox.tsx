import View from "./View";

export default function () {
    return (
        <View style={{
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'center',
        }}>
            <input type="checkbox" />
        </View>
    )
}